{
  "name": "project",
  "version": "1.0.0",
  "description": "",
  "main": "src/server.ts",
  "scripts": {
    "start": "tsx src/server.ts",
    "pull": "git fetch && git pull && yarn install && pm2 restart server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "tsx": "^3.12.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.14"
  }
}
